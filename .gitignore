# macOS
.DS_Store

# Editors / IDEs
.idea/
.vscode/

# Local leftovers (no Python/Node build for this project)
venv/
.venv/
ENV/
env/
dist/
build/
*.zip

# Claude Code local state — agent worktrees are checkouts of this same repo
# and must never be committed into it.
.claude/worktrees/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables
.env
.env.local
.env.*.local

# Logs
/logs/
*.log

# Playwright Testing (local artifacts — never push)
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Accidental Cursor sandbox cache drop (directory literally named ")
"/
.playwright-browsers/
